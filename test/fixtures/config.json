{
  "name": "fixture-config",
  "vus": 2,
  "duration": "2s",
  "scenarios": [
    {
      "name": "health",
      "method": "GET",
      "url": "{{baseUrl}}/health",
      "checks": ["status:200"]
    }
  ]
}
